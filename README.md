# Expo Scan Backend

Backend Node/Express para la app de escaneo de gafetes, registros de contactos, usuarios y chat interno en tiempo real.

## Scripts

```bash
npm install
npm run build
npm start
```

## Variables de entorno

Crea un archivo `.env` basado en `.env.example` antes de correr el proyecto.
